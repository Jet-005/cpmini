<van-tabbar active="{{ active }}" wx:if="{{ show }}" bind:change="onChange" safe-area-inset-bottom="true">
  <van-tabbar-item data-path="{{item.path}}" name="{{item.key}}" wx:for="{{tabbar[curRole]}}" wx:key="key" icon="{{item.icon}}">{{item.name}}</van-tabbar-item>
</van-tabbar>