<view class="not_login_box" wx:if="{{ !hasRole }}">
  <login bind:login="doLogin" id="loginCompoentRef"></login>
</view>
<view wx:else>
  <user wx:if="{{curRole === 'user'}}"></user>
  <manager wx:else>
  </manager>
</view>